{
  "name": "closeloop",
  "version": "0.1.0",
  "private": true,
  "description": "Unsold-estimate recovery and lead follow-up system for high-ticket trades.",
  "type": "commonjs",
  "main": "src/server.js",
  "engines": { "node": ">=22.5.0" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node src/db/seed.js",
    "test": "node --test 'tests/*.test.js'",
    "reset": "rm -f data/closeloop.db && node src/db/seed.js"
  },
  "dependencies": {}
}
