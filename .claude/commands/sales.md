---
description: Review or improve the sales system
---

Delegate to the `growth` agent. Read `business/GROWTH.md`, `business/sales/`, and
`business/data/pipeline.json` first.

Report: measured conversion at every stage, where the funnel leaks worst, and the one change most
likely to fix it. Then make that change to the sequence, script, or offer.

Remember the boundary: we draft, the founder sends. Output is always send-ready, never sent.