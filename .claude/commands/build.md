---
description: Build or spec product work
---

Delegate to the `builder` agent. Read `business/PRODUCT.md` first.

The builder checks the validation gate in `business/MASTER_PLAN.md` before writing any product code.
Gate not passed -> specification only, and say so plainly.

Every proposed feature must answer: does this acquire, convert, serve, retain, or scale customers?
If not, it goes to the Deferred list with a one-line reason. Free tiers and stdlib only. No secrets
in the repo.