---
description: Refresh and report all KPIs
---

Run `node tools/kpi.js`, `node tools/ledger.js`, and `node tools/pipeline.js`, then read
`business/KPI_DASHBOARD.md`.

Report revenue (MRR, this month, growth), customers (leads, qualified, conversations, customers,
churn, retention), sales (outreach volume, reply rate, booking rate, close rate, average deal value,
CAC), and operations (fulfillment time, cost per customer, gross margin, automation percentage).

Close with progress toward $10,000/month and the metric most off-plan. Print `not yet measured`
for anything unmeasured - never estimate into a KPI slot.