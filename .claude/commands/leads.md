---
description: Show, verify, or build the prospect list
---

Read `business/customers/leads.csv` and `business/data/leads.json`.

Report: total leads, how many contacted, replied, booked, won; and the next batch the founder should
send today (20-40 rows, highest fit first) with the exact message body for each.

To add leads, delegate to the `growth` agent. Public sources only. Every row needs a real business
name, a real URL, and a verifiable contact path. Spot-check three new rows by fetching their site
before handing the list over. Never invent a contact.